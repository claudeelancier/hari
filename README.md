# VeltroHR

Marketing landing page for VeltroHR — HR management software.

## Open locally

Open `index.html` in a browser, or serve the folder:

```bash
python3 -m http.server 4173
```

Then visit http://localhost:4173
