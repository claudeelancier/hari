# Hari

Cinematic 3D hero for a software-engineer portfolio.

```bash
npm install
npm run dev
```

Open `http://localhost:5173`. Move the pointer across the hero — the iridescent core, rings, and camera respond in realtime.
